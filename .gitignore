# Godot 4 editor/import cache — regenerated by `godot --headless --import`
.godot/

# Local editor/tooling config. Project conventions are tracked in
# docs/conventions.md; these files only point at it.
CLAUDE.md
AGENTS.md
.claude/

# Exported builds
build/
export_presets.cfg

# OS / editor cruft
.DS_Store
Thumbs.db
*.tmp
